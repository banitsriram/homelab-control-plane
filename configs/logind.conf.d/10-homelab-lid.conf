# /etc/systemd/logind.conf.d/10-homelab-lid.conf
# Keep running headless with the lid shut, on battery or mains.
# Applied by setup.sh (step: lid). Reload with: systemctl restart systemd-logind
[Login]
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore
