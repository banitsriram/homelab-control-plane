# Copy to .env and fill in. This is for the node-level services in
# docker-compose.yml — Kairos keeps its own .env in the Kairos checkout.
TZ=Asia/Kolkata

# Cloudflare Tunnel token (Zero Trust dashboard → Tunnels → your tunnel).
# Only needed for the `ingress` profile. Leave blank otherwise.
TUNNEL_TOKEN=
